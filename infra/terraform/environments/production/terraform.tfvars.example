aws_region        = "il-central-1"
ami_id            = "ami-xxxxxxxxxxxxxxxxx"   # Ubuntu 24.04 LTS in il-central-1
key_pair_name     = "maple-prod-key"
ssh_allowed_cidrs = ["YOUR_IP/32"]            # Never use 0.0.0.0/0 in production
