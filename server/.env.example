# =============================================================================
# Maple Garden Events — Server Environment (copy to .env and fill in values)
# =============================================================================

# --- Required ---
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
JWT_SECRET=change-me-to-a-long-random-string
GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com

# --- Server ---
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173
SERVE_CLIENT=false

# --- Email (optional — without these, emails are simulated/logged) ---
EMAIL_USER=
EMAIL_PASS=
MANAGER_EMAIL=

# --- WhatsApp Green API (optional) ---
GREEN_API_INSTANCE_ID=
GREEN_API_TOKEN=

# --- Twilio SMS (optional) ---
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# --- Sentry (optional) ---
SENTRY_DSN=
SENTRY_TRACES_SAMPLE_RATE=0.1
TENANT_NAME=maple-garden

# --- Database backups (optional) ---
BACKUP_ENABLED=false
BACKUP_CRON=0 3 * * *
BACKUP_DIR=./backups
BACKUP_RETENTION_COUNT=7
S3_BACKUP_BUCKET=
S3_BACKUP_PREFIX=database-backups
BACKUP_RETENTION_DAYS=30
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=auto
S3_ENDPOINT=

# --- Logging ---
LOG_DIR=./logs
LOG_LEVEL=info
